# 🔑 Asymmetric Encryption - Mã hóa Bất đối xứng

> Thuật toán sử dụng **cặp khóa** public/private.

---

## 📊 Tổng quan

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Asymmetric Encryption Flow                       │
└─────────────────────────────────────────────────────────────────────┘

    Alice (Sender)                              Bob (Receiver)
    ──────────────                              ───────────────
    
                                          ┌─────────────────────┐
                                          │   Key Generation    │
                                          │   ┌───────────────┐ │
                                          │   │ Private Key   │ │ ← SECRET!
                                          │   │ (sk)          │ │
                                          │   └───────────────┘ │
                                          │   ┌───────────────┐ │
                                          │   │ Public Key    │ │ ← Can share
                                          │   │ (pk)          │ │
                                          │   └───────────────┘ │
                                          └──────────┬──────────┘
                                                     │
              pk (public key) ◄──────────────────────┘
                   │
                   ▼
    ┌──────────────────────┐
    │ Encrypt with pk      │
    │ E(pk, message) = ct  │
    └──────────┬───────────┘
               │
               ▼
         Ciphertext (ct) ─────────────────────────▶ ┌──────────────────┐
                                                    │ Decrypt with sk  │
                                                    │ D(sk, ct) = msg  │
                                                    └──────────────────┘
                                                             │
                                                             ▼
                                                         Message

    ✅ No need to share secret key!
    ✅ Anyone can encrypt with public key
    ✅ Only private key holder can decrypt
```

---

## 📋 Thuật toán trong FileVault

| File | Algorithm | Type | Key Sizes | Use Case |
|------|-----------|------|-----------|----------|
| [rsa.md](./rsa.md) | RSA-OAEP | Encryption | 2048, 3072, 4096 | Key exchange, legacy |
| [ecc.md](./ecc.md) | ECDH + AES | Hybrid | P-256, P-384, P-521 | Modern key exchange |

---

## 🔐 RSA vs ECC

```
┌─────────────────────────────────────────────────────────────────────┐
│                    RSA vs ECC Comparison                            │
└─────────────────────────────────────────────────────────────────────┘

                    RSA-3072         ECC-P256         Kyber-768
                    ────────         ────────         ─────────
Security level      128-bit          128-bit          192-bit (PQ)
Public key          384 bytes        65 bytes         1184 bytes
Private key         ~1700 bytes      32 bytes         2400 bytes
KeyGen time         ~150ms           ~0.3ms           ~0.08ms
Encrypt time        ~0.5ms           ~0.4ms           ~0.09ms
Quantum safe?       ❌ No            ❌ No            ✅ Yes

Recommendation:
├── Legacy systems: RSA-3072+
├── Modern systems: ECC-P256 or X25519
└── Future-proof: Kyber (PQC)
```

---

## ⚛️ Quantum Threat

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Shor's Algorithm Threat                          │
└─────────────────────────────────────────────────────────────────────┘

    Shor's Algorithm (Peter Shor, 1994):
    ├── Factors large integers in polynomial time
    ├── Solves discrete log in polynomial time
    └── Breaks RSA, DH, ECC, ECDSA, EdDSA!
    
    Current status:
    ┌─────────────────┬────────────────────────────────────────┐
    │   Year          │   Largest number factored              │
    ├─────────────────┼────────────────────────────────────────┤
    │   2001          │   15 = 3 × 5 (7 qubits)                │
    │   2012          │   21 = 3 × 7                           │
    │   2019          │   35 = 5 × 7                           │
    │   2023          │   ~50 bits (classical preprocessing)   │
    └─────────────────┴────────────────────────────────────────┘
    
    RSA-2048 needs: ~4000 logical qubits
    Timeline: Unknown, possibly 10-20 years
    
    ⚠️ "Harvest Now, Decrypt Later"
    → Use PQC (Kyber) for sensitive data!
```

---

## 🔧 Use Cases

### 1. Key Exchange

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Hybrid Encryption Pattern                        │
└─────────────────────────────────────────────────────────────────────┘

    Problem: RSA is SLOW and has size limits
    Solution: Hybrid encryption

    1. Generate random AES key (32 bytes)
    2. Encrypt AES key with RSA/ECC
    3. Encrypt data with AES-GCM
    4. Send both encrypted key + ciphertext

    ┌─────────────────────────────────────────────────────────────┐
    │                                                             │
    │  Random AES Key ───┬──▶ RSA(pk) ──▶ Encrypted Key           │
    │       (32B)        │                   (~256B)              │
    │                    │                                        │
    │                    └──▶ AES-GCM ──▶ Encrypted Data          │
    │                                       (any size)            │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
```

### 2. Digital Signatures

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Digital Signature Flow                           │
└─────────────────────────────────────────────────────────────────────┘

    Signer (has private key)              Verifier (has public key)
    ────────────────────────              ─────────────────────────
    
    Document ──▶ Hash ──▶ Sign(sk) ──▶ Signature
                                           │
                                           ▼
               ┌─────────────────────────────────────────┐
               │ Document + Signature sent to verifier   │
               └─────────────────────────────────────────┘
                                           │
                                           ▼
                           Verify(pk, document, signature)
                                           │
                              ┌────────────┴────────────┐
                              ▼                         ▼
                           VALID                     INVALID
                        (authentic)                (tampered!)
```

---

## 💻 Sử dụng trong FileVault

```bash
# Generate RSA keypair
filevault keygen -a rsa-4096 -o mykey

# Generate ECC keypair
filevault keygen -a ecc-p521 -o mykey

# Encrypt with public key
filevault encrypt secret.txt --pubkey mykey.pub

# Decrypt with private key
filevault decrypt secret.txt.fvlt --privkey mykey.key

# Sign a file
filevault sign document.pdf --privkey mykey.key

# Verify signature
filevault verify document.pdf --pubkey mykey.pub --sig document.pdf.sig
```

---

## 📐 Mathematical Foundations

### RSA

```
Key Generation:
───────────────
1. Choose large primes p, q (~1536 bits each for RSA-3072)
2. Compute n = p × q
3. Compute φ(n) = (p-1)(q-1)
4. Choose e = 65537 (common choice)
5. Compute d = e⁻¹ mod φ(n)

Public key:  (n, e)
Private key: (n, d)

Encryption:  c = m^e mod n
Decryption:  m = c^d mod n

Security: Based on integer factorization problem
```

### ECC (Elliptic Curve)

```
Curve equation: y² = x³ + ax + b (mod p)

Key Generation:
───────────────
1. Choose random private key d ∈ [1, n-1]
2. Compute public key Q = d × G

Where:
├── G = generator point (fixed for curve)
├── n = order of curve
└── × = elliptic curve point multiplication

ECDH Key Exchange:
──────────────────
Alice: private a, public A = a×G
Bob:   private b, public B = b×G

Shared secret: a×B = b×A = ab×G

Security: Based on ECDLP (Elliptic Curve Discrete Log Problem)
```

---

## 📚 Tài liệu tham khảo

1. **PKCS#1** - RSA Cryptography Standard
2. **SEC 1** - Elliptic Curve Cryptography
3. **RFC 5208** - PKCS#8 Private Key Format
4. **RFC 5280** - X.509 Certificate Format

---

<div align="center">

**[← Quay lại Algorithms](../README.md)**

</div>
