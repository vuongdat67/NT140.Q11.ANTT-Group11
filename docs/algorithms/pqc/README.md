# 🛡️ Post-Quantum Cryptography (PQC)

> Thuật toán **chống lại máy tính lượng tử** - NIST Selected Standards.

---

## ⚛️ Tại sao cần PQC?

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Quantum Threat Timeline                          │
└─────────────────────────────────────────────────────────────────────┘

    2024        2030?       2035?       2040?
      │           │           │           │
      ▼           ▼           ▼           ▼
    ┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐
    │ Now │    │ ~50 │    │~1000│    │Crypto│
    │     │    │qubit│    │qubit│    │ Rel. │
    │ R&D │    │     │    │     │    │ QC   │
    └─────┘    └─────┘    └─────┘    └─────┘
    
    Thuật toán Shor sẽ phá:
    ├── RSA (mọi key size)
    ├── ECC (P-256, P-384, P-521)
    ├── Diffie-Hellman
    └── ECDSA, EdDSA
    
    ⚠️ "Harvest Now, Decrypt Later":
    Kẻ tấn công thu thập encrypted data BÂY GIỜ,
    chờ quantum computer → giải mã TẤT CẢ!
```

---

## 📋 Thuật toán trong FileVault

### 🔑 Key Encapsulation (KEM)

| File | Algorithm | Type | NIST Level | Use Case |
|------|-----------|------|------------|----------|
| [kyber.md](./kyber.md) | **Kyber** (ML-KEM) | Lattice | 1, 3, 5 | Key exchange, hybrid encryption |

### ✍️ Digital Signatures

| File | Algorithm | Type | NIST Level | Use Case |
|------|-----------|------|------------|----------|
| [dilithium.md](./dilithium.md) | **Dilithium** (ML-DSA) | Lattice | 2, 3, 5 | Code signing, authentication |

---

## 🔬 NIST PQC Competition

```
┌─────────────────────────────────────────────────────────────────────┐
│                    NIST PQC Standards (2024)                        │
└─────────────────────────────────────────────────────────────────────┘

    FIPS 203: ML-KEM (Kyber)
    ├── Module-Lattice-based Key Encapsulation
    ├── Kyber-512, Kyber-768, Kyber-1024
    └── Primary standard for key exchange

    FIPS 204: ML-DSA (Dilithium)
    ├── Module-Lattice-based Digital Signature
    ├── Dilithium-2, Dilithium-3, Dilithium-5
    └── Primary standard for signatures

    FIPS 205: SLH-DSA (SPHINCS+)
    ├── Stateless Hash-based Signature
    ├── Conservative, hash-only construction
    └── Backup option (larger signatures)
```

---

## 📊 So sánh Security Levels

```
┌──────────────────────────────────────────────────────────────────────┐
│                    NIST Security Levels                              │
├───────────┬─────────────┬─────────────┬─────────────────────────────┤
│   Level   │  Classical  │   Quantum   │        FileVault            │
├───────────┼─────────────┼─────────────┼─────────────────────────────┤
│  Level 1  │   AES-128   │  128-bit    │  Kyber-512                  │
│  Level 2  │   SHA-256   │  128-bit    │  Dilithium-2                │
│  Level 3  │   AES-192   │  192-bit    │  Kyber-768, Dilithium-3     │
│  Level 5  │   AES-256   │  256-bit    │  Kyber-1024, Dilithium-5    │
└───────────┴─────────────┴─────────────┴─────────────────────────────┘

Khuyến nghị:
├── Default: Level 3 (Kyber-768)
├── Maximum security: Level 5 (Kyber-1024)
└── Performance critical: Level 1 (Kyber-512)
```

---

## 🔗 Hybrid Mode

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Hybrid Encryption Strategy                       │
└─────────────────────────────────────────────────────────────────────┘

    FileVault sử dụng HYBRID approach:
    
    ┌─────────────┐        ┌─────────────┐
    │   Kyber     │        │  Password   │
    │   (PQC)     │        │  + Argon2   │
    └──────┬──────┘        └──────┬──────┘
           │                      │
           ▼                      ▼
    ┌─────────────┐        ┌─────────────┐
    │  Shared     │        │  Classical  │
    │  Secret     │        │    Key      │
    └──────┬──────┘        └──────┬──────┘
           │                      │
           └──────────┬───────────┘
                      │
                      ▼
               ┌─────────────┐
               │    HKDF     │
               │   Combine   │
               └──────┬──────┘
                      │
                      ▼
               ┌─────────────┐
               │ AES-256-GCM │
               │   Encrypt   │
               └─────────────┘
    
    Security = MAX(Kyber, Classical)
    ├── Nếu Kyber bị phá → Classical vẫn bảo vệ
    ├── Nếu Classical bị phá → Kyber vẫn bảo vệ
    └── Cần phá CẢ HAI để decrypt!
```

---

## 🔢 So sánh Key/Ciphertext Sizes

```
┌──────────────────────────────────────────────────────────────────────┐
│                    Key & Ciphertext Sizes                            │
├───────────────────┬───────────┬───────────┬───────────┬─────────────┤
│    Algorithm      │Public Key │Private Key│Ciphertext │ Security    │
├───────────────────┼───────────┼───────────┼───────────┼─────────────┤
│ RSA-3072          │   384 B   │  ~1700 B  │   384 B   │ 128-bit     │
│ ECC-P256          │    65 B   │    32 B   │    65 B   │ 128-bit     │
├───────────────────┼───────────┼───────────┼───────────┼─────────────┤
│ Kyber-512         │   800 B   │  1632 B   │   768 B   │ 128-bit PQ  │
│ Kyber-768         │  1184 B   │  2400 B   │  1088 B   │ 192-bit PQ  │
│ Kyber-1024        │  1568 B   │  3168 B   │  1568 B   │ 256-bit PQ  │
├───────────────────┼───────────┼───────────┼───────────┼─────────────┤
│ Dilithium-2       │  1312 B   │  2560 B   │  2420 B   │ 128-bit PQ  │
│ Dilithium-3       │  1952 B   │  4032 B   │  3293 B   │ 192-bit PQ  │
│ Dilithium-5       │  2592 B   │  4896 B   │  4595 B   │ 256-bit PQ  │
└───────────────────┴───────────┴───────────┴───────────┴─────────────┘

⚠️ PQC keys/ciphertexts lớn hơn classical!
   Trade-off cho quantum resistance
```

---

## ⚡ Performance

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PQC Benchmark (FileVault)                        │
└─────────────────────────────────────────────────────────────────────┘

$ filevault benchmark --pqc

┌─────────────────────┬─────────────┬────────────┬───────────────────┐
│      Algorithm      │   KeyGen    │  Encaps    │     Decaps        │
├─────────────────────┼─────────────┼────────────┼───────────────────┤
│ Kyber-512           │   0.05 ms   │  0.06 ms   │     0.07 ms       │
│ Kyber-768           │   0.08 ms   │  0.09 ms   │     0.10 ms       │
│ Kyber-1024          │   0.12 ms   │  0.13 ms   │     0.15 ms       │
├─────────────────────┼─────────────┼────────────┼───────────────────┤
│ Dilithium-2         │   0.15 ms   │  0.08 ms   │     0.12 ms       │
│ Dilithium-3         │   0.25 ms   │  0.13 ms   │     0.18 ms       │
│ Dilithium-5         │   0.40 ms   │  0.20 ms   │     0.28 ms       │
├─────────────────────┼─────────────┼────────────┼───────────────────┤
│ RSA-3072 (classic)  │  150 ms     │  0.5 ms    │     5.0 ms        │
│ ECC-P256 (classic)  │   0.3 ms    │  0.4 ms    │     0.4 ms        │
└─────────────────────┴─────────────┴────────────┴───────────────────┘

✅ Kyber KeyGen nhanh hơn RSA ~1000x!
✅ Kyber operations nhanh hơn ECC!
```

---

## 📖 Cách sử dụng trong FileVault

### Generate Kyber Keypair

```bash
# Generate Kyber-1024 keypair
filevault keygen -a kyber-1024 -o my-quantum-key

# Output:
# my-quantum-key.pub  (1568 bytes - public key)
# my-quantum-key.key  (3168 bytes - private key, KEEP SECRET!)
```

### Encrypt with Kyber-Hybrid

```bash
# Encrypt file with Kyber + password
filevault encrypt secret.txt -a kyber-1024-hybrid

# This:
# 1. Prompts for password
# 2. Generates random Kyber session key
# 3. Combines Kyber shared secret + password-derived key
# 4. Encrypts with AES-256-GCM
```

### Decrypt

```bash
# Decrypt (needs password + private key)
filevault decrypt secret.txt.fvlt --privkey my-quantum-key.key
```

---

## 🔬 Underlying Math

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Lattice-based Crypto                             │
└─────────────────────────────────────────────────────────────────────┘

Kyber và Dilithium dựa trên Module-LWE problem:

    Given: (A, b = A·s + e)
    Find:  s
    
    Với:
    ├── A: random matrix (public)
    ├── s: secret vector
    ├── e: small error vector (noise)
    └── Operations in Rq = Zq[X]/(X^256 + 1)

    NP-hard related problem!
    ├── No known quantum algorithm solves efficiently
    ├── Studied for 30+ years
    └── Basis for NIST standards

Chi tiết xem: [kyber.md](./kyber.md)
```

---

## 📚 Tài liệu tham khảo

1. **NIST PQC** - https://csrc.nist.gov/projects/post-quantum-cryptography
2. **FIPS 203** - ML-KEM Standard
3. **CRYSTALS-Kyber** - https://pq-crystals.org/kyber/
4. **CRYSTALS-Dilithium** - https://pq-crystals.org/dilithium/

---

<div align="center">

**[← Quay lại Algorithms](../README.md)**

</div>
